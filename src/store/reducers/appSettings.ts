const initialState = {
  isMusicOn: true,
  isVibrationON: true,
  isSoundsOn: true,
  isAdsON: true,
  selectedLanguage: "en",
};

const appSettingsReducer = (state = initialState, action: any) => {
  return state;
};

export default appSettingsReducer;
