const initialState = {
  isMusicOn: true,
  isVibrationON: true,
  isSoundsOn: true,
  isAdsON: true,
};

const settingsReducer = (state = initialState, action) => {
  return state;
};

export default settingsReducer;
